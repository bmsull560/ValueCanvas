FROM caddy:2-alpine

# Copy Caddyfile
COPY infrastructure/caddy/Caddyfile /etc/caddy/Caddyfile

# Expose ports
EXPOSE 80 443

# Use default Caddy command
CMD ["caddy", "run", "--config", "/etc/caddy/Caddyfile", "--adapter", "caddyfile"]
