# Production environment variables
PROD_DB_NAME=myapp_prod
PROD_DB_USER=postgres
PROD_DB_PASSWORD=${vault://secret/data/production/database#password}
PROD_DATABASE_URL=postgresql://${PROD_DB_USER}:${PROD_DB_PASSWORD}@prod-postgres:5432/${PROD_DB_NAME}

REDIS_PASSWORD=${vault://secret/data/production/redis#password}
PROD_REDIS_URL=redis://:${REDIS_PASSWORD}@prod-redis:6379

# Vault Configuration
VAULT_ENABLED=true
VAULT_ADDR=https://vault.company.com
VAULT_NAMESPACE=valuecanvas

PORT=8000
NODE_ENV=production
