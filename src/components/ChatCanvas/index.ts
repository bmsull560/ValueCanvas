/**
 * ChatCanvas Components
 * 
 * Main application layout following the chat + canvas pattern:
 * - Library sidebar for case management
 * - Canvas for SDUI rendered content
 * - Command bar for AI interaction
 */

export { ChatCanvasLayout } from './ChatCanvasLayout';
export type { default as ChatCanvasLayoutType } from './ChatCanvasLayout';
