# Helm values for secrets-store-csi-driver
# Enables secret sync and rotation so pod-mounted files stay fresh.

syncSecret:
  enabled: true

enableSecretRotation: true
rotationPollInterval: "2m"

linux:
  enabled: true
  podDisruptionBudget:
    enabled: true

windows:
  enabled: false
