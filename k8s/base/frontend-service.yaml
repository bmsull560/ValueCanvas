apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: valuecanvas
  labels:
    app: frontend
    component: web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: frontend
