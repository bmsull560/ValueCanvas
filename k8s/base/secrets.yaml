# This is a template file. Actual secrets should be created using:
# kubectl create secret generic valuecanvas-secrets \
#   --from-literal=database-url=<value> \
#   --from-literal=supabase-url=<value> \
#   --from-literal=supabase-anon-key=<value> \
#   --from-literal=supabase-service-key=<value> \
#   --from-literal=together-api-key=<value> \
#   --from-literal=openai-api-key=<value> \
#   --from-literal=jwt-secret=<value> \
#   --namespace=valuecanvas

# Or use AWS Secrets Manager with External Secrets Operator
# See: k8s/base/external-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: valuecanvas-secrets
  namespace: valuecanvas
  labels:
    app.kubernetes.io/name: valuecanvas
    app.kubernetes.io/part-of: valuecanvas
type: Opaque
stringData:
  # These are placeholders - replace with actual values or use External Secrets
  database-url: "REPLACE_ME"
  supabase-url: "REPLACE_ME"
  supabase-anon-key: "REPLACE_ME"
  supabase-service-key: "REPLACE_ME"
  together-api-key: "REPLACE_ME"
  openai-api-key: "REPLACE_ME"
  jwt-secret: "REPLACE_ME"
