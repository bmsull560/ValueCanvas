---
trigger: always_on
---

# Frontend Components

**Paths:** `src/sdui/*` & `src/components/Agent/*`

- Components must be in `ui-registry.json` and `src/sdui/registry.tsx`
- AI content must be visually distinct (`GhostPreview` or "AI Generated" badges)
- Use `useRealtimeUpdates` for WebSocket subscriptions
